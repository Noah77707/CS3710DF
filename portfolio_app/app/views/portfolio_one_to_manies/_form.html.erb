<%= form_with(model: portfolio_one_to_many) do |form| %>
  <% if portfolio_one_to_many.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(portfolio_one_to_many.errors.count, "error") %> prohibited this portfolio_one_to_many from being saved:</h2>

      <ul>
        <% portfolio_one_to_many.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.submit %>
  </div>
<% end %>
