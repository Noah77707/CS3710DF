<p style="color: green"><%= notice %></p>

<h1>Students</h1>

<%= render 'search_form', search_params: @search_params %>

<div id="students">
  <% @students.each do |student| %>
    <%= render student %>
  <% end %>
</div>
